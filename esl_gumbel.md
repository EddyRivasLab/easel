# esl_gumbel - Type I extreme value (Gumbel) statistics

Gumbel statistics are often used to estimate the statistical
significance of local alignment scores.

The Gumbel distribution is the so-called Type I extreme value
distribution (EVD). It occurs so frequently in sequence analysis
applications, compared to the type II (Fréchet) and type III (Weibull)
extreme value distributions, that "Gumbel" and "EVD" are often used
interchangeably in bioinformatics. Easel has a separate module, the
`gev` module, that implements the generalized extreme value
distribution.

Karlin/Altschul statistics are a special case of the Gumbel
distribution that apply to the scores of ungapped local alignments
between infinitely long random sequences. Empirically, Karlin/Altschul
statistics also apply reasonably well to the more useful case of
gapped alignment of finite-length sequences. Karlin/Altschul
statistics predict how the Gumbel's two parameters depend on the
length of the query and target sequences. In the case of ungapped
alignments, Karlin/Altschul statistics allow the Gumbel parameters to
be estimated directly, without the need for a compute-intensive
simulation.

The Gumbel distribution depends on two parameters, $\mu$ and
$\lambda$. When $\mu$ and $\lambda$ are known, the statistical
significance (P-value) of a single score $x$ is $P(S>x)$, obtained by
a call to `esl_gumbel_surv()`. The E-value for obtaining that score or
better in searching a database of $N$ sequences is just $NP(S>x)$.

When $\mu$ and $\lambda$ are unknown, they are estimated from scores
obtained from comparisons of simulated random data. (Analytical
solutions for $\mu$ and $\lambda$ are only available in the case of
ungapped sequence alignments.) The `esl_gumbel_Fit*()` functions
provide maximum likelihood parameter fitting routines for different
types of data.

## Gumbel densities

The probability density function (pdf) and the cumulative distribution
function (cdf) of the extreme value distribution are:

$$
P(x) = \lambda \exp \left[ -\lambda (x - \mu) - e^{- \lambda (x - \mu)} \right] \tag{1}
$$

$$
P(S < x) = \exp \left[ -e^{-\lambda(x - \mu)} \right] \tag{2}
$$


For more details, a classic reference is [Lawless82]. Gumbel
distributions can have their long tail to the right or to the left.
The form given here is for the long tail to the right. This is the
form that arises when the extreme value is a maximum, such as when our
score is the maximum over the individual scores of all possible
alignments. The equations in [Lawless82] are for extremal minima; use
$(x - u) = -(x - \mu)$ and $b = 1 / \lambda$ to convert Lawless'
notation to the notation used here.


## Fitting Gumbel distributions to observed data

Given a set of $n$ observed samples $\mathbf{x}$, we may want to
estimate the $\mu$ and $\lambda$ parameters.

One might try to use linear regression to fit to a $\log \log$
transformation of the $P(S < x)$ histogram, which gives a straight
line with slope $-\lambda$ and $x$ intercept $\mu$:

$$
\log \left[ -\log P(S<x) \right] = -\lambda x + \lambda \mu \tag{3}
$$

However, the linear regression method is undesirable because it is
sensitive to outliers. The following table shows the % error for
estimating $\hat{\mu}$ and $\hat{\lambda}$ from 500 simulated complete
datasets, sampled from a Gumbel with $\mu = -20.0$ and $\lambda = 0.4$,
for four different dataset sizes (columns are the # of samples):

|                              | 100 | 1000  | 10,000 | 100,000 |
|------------------------------|-----|-------|--------|---------|
| % error in $\hat{\mu}$       | 2%  | 1%    | 0.9%   | 0.9%    |
| max error in $\hat{\mu}$     | 24% | 13%   | 10%    | 10%     |
| % error in $\hat{\lambda}$   | 12% | 7%    | 5%     | 3%      |
| max error in $\hat{\lambda}$ | 49% | 33%   | 25%    | 20%     |

A better rough estimate of $\hat{\mu}$ and $\hat{\lambda}$ can be
obtained from the sample mean $m$ and variance $s^2$ of the observed
data [Evans00]:

$$
\begin{aligned}
  \hat{\lambda} & = \frac{\pi}{\sqrt{6s^2}}\\
  \hat{\mu}     & = m - \frac{0.57722}{\hat{\lambda}}
\end{aligned}
$$

(All simulation data are generated by the `evd` module's stats driver.
The only exception is the linear regression fit data, which come from
an old version of HMMER.)

The mean/variance method is more accurate than linear regression, as
shown by the following simulation results (columns are the # of
samples):

|                              | 100 | 1000  | 10,000 | 100,000 |
|------------------------------|-----|-------|--------|---------|
| % error in $\hat{\mu}$       | 1%  | 0.3%  | 0.1%   | 0.03%   |
| max error in $\hat{\mu}$     | 5%  | 1%    | 0.4%   | 0.1%    |
| % error in $\hat{\lambda}$   | 9%  | 3%    | 0.8%   | 0.3%    |
| max error in $\hat{\lambda}$ | 40% | 12%   | 3%     | 0.9%    |

Still, the mean/variance method is not as accurate as a maximum
likelihood estimation (especially for $\lambda$). Also, it requires
complete data, whereas we also need to solve problems where we fit to
_truncated_ or _censored_ data. Easel's main estimation methods are
therefore maximum likelihood methods.


### maximum likelihood estimation, complete data

Given $n$ samples $x_1..x_n$ from some distribution that depends on
parameters $\theta$, we want to estimate maximum likelihood parameter
estimates $\hat{\theta}$ that maximize the log likelihood:

$$
   \hat{\theta} = \mathop{\mathrm{argmax}}_{\theta} \sum_{i=1}^{n} \log P(x_i \mid \theta)
$$

These are also _maximum a posteriori_ parameter estimates, if we
assume a uniform prior $P(\theta)$.

Specifically, for samples $x_i$ drawn from an extreme value
distribution, the log likelihood to optimize is:

$$
\log L(\lambda, \mu) = n \log \lambda - \sum_{i=1}^{n} \lambda(x_i -
\mu) - \sum_{i=1}^{n} e^{-\lambda(x_i - \mu)} \tag{4}
$$

This objective function is differentiable with respect to $\mu$ and
$\lambda$:

$$
\frac{\partial \log L}{\partial \mu} =
n \lambda - \lambda \sum_{i=1}^{n} e^{-\lambda (x_i - \mu)} \tag{5}
$$
$$
\frac{\partial \log L}{\partial \lambda} =
\frac{n}{\lambda} - \sum_{i=1}^{n} (x_i - \mu) +
\sum_{i=1}^{n} (x_i - \mu) e^{-\lambda (x_i - \mu)} \tag{6}
$$

The maximum likelihood estimates $\hat{\lambda}$ and $\hat{\mu}$ are
the solutions to $\frac{\partial \log L}{\partial \mu} = 0$ and
$\frac{\partial \log L}{\partial \lambda} = 0$. Lawless [Lawless82]
gives a useful trick here that lets us solve both of these
simultaneously. When equation (5) is set to zero, it can be used to
get $\hat{\mu}$ in terms of $\hat{\lambda}$:

$$
e^{-\hat{\lambda} \hat{\mu}} = \frac{1}{n} \sum_{i=1}^{n} e^{-\hat{\lambda} x_i} \tag{7}
$$
$$
\hat{\mu} = - \frac{1}{\hat{\lambda}}
	\log \left[ \frac{1}{n} \sum_{i=1}^{n} e^{-\hat{\lambda} x_i} \right] \tag{8}
$$

Substituting (7) into (6) gives us an equation for solving
$\hat{\lambda}$ in terms of the $x_i$'s:

$$
\frac{1}{\hat{\lambda}} - \frac{1}{n} \sum_{i=1}^{n} x_i +
\frac{\sum_{i=1}^{n} x_i e^{-\hat{\lambda} x_i}}
     {\sum_{i=1}^{n} e^{-\hat{\lambda} x_i}}
= 0 \tag{9}
$$

This is our target function. We could solve it readily enough (by
bisection search, for example) and obtain $\hat{\lambda}$. We can solve
it even faster using the Newton/Raphson algorithm, because it is
differentiable with respect to lambda:

$$
\frac{d}{d\hat{\lambda}} =
\frac{\left( \sum_{i=1}^{n} x_i e^{-\hat{\lambda} x_i} \right)^2 }
     {\left( \sum_{i=1}^{n} e^{-\hat{\lambda} x_i}     \right)^2 }
-
\frac{\sum_{i=1}^{n} x_i^2 e^{-\hat{\lambda} x_i}}
     {\sum_{i=1}^{n} e^{-\hat{\lambda} x_i}}
-
\frac{1}{\hat{\lambda}^2} \tag{10}
$$

Now, the key equations are (8), (9), and (10). In summary, the
inference procedure is the following:

* Guess an initial $\hat{\lambda}$ (using the mean/variance method, for
  example, but any reasonable guess works).
* Use Newton/Raphson iterations to find the $\hat{\lambda}$ that
  satisfies (9):
    * calculate the target function $f$ and its first derivative $f'$
      at $\hat{\lambda}$, using (9) to calculate $f$ and (10) to
      calculate $f'$.
    * If $f$ is within some absolute tolerance of zero (e.g.,
      $10^{-6}$), stop; we have found $\hat{\lambda}$.
    * Else, estimate a new $\hat{\lambda} = \hat{\lambda} -
      \frac{f}{f'}$, and do another iteration.
* Plug $\hat{\lambda}$ into (8) to get $\hat{\mu}$.

This algorithm is implemented in `esl_gumbel_FitComplete()`. An
auxiliary function, `lawless416()`, calculates the target function and
its derivative (equations (9) and (10)) given the current estimate of
$\hat{\lambda}$. The name comes from Lawless' equation 4.1.6, the
target function [Lawless82].

The accuracy of fitting to simulated data (generated with $\mu=-20$ and
$\lambda=0.4$), collated over 500 simulations, is shown in the
following table (columns are the # of samples):

|                              | 100 | 1000  | 10,000 | 100,000 |
|------------------------------|-----|-------|--------|---------|
| % error in $\hat{\mu}$       | 1%  | 0.3%  | 0.1%   | 0.03%   |
| max error in $\hat{\mu}$     | 4%  | 2%    | 0.5%   | 0.1%    |
| % error in $\hat{\lambda}$   | 6%  | 2%    | 0.6%   | 0.2%    |
| max error in $\hat{\lambda}$ | 36% | 9%    | 2%     | 0.8%    |

This is in accord with theoretical expectation. The distribution of
$\frac{\lambda}{\hat{\lambda}}$ is approximately normal with mean 1 and
standard error $\frac{0.78}{\sqrt{N}}$ [Lawless82, Altschul01].


### maximum likelihood fitting to censored data

A _censored_ data problem is when we have $N$ samples, but we only
observe the values of a subset of $n$ samples $x_1..x_n$ that are
greater or equal to some cutoff $\phi$. The remaining $z = N-n$ samples
are _censored_, and for these we only know that $x < \phi$. $x_i..x_n$,
$n$, $\phi$, and $z$ are all known in a censored data problem.

To estimate maximum likelihood parameters $\hat{\theta}$ for some
distribution from censored data [Gelman95], the log likelihood to
maximize is:

$$
  \hat{\theta} = \mathop{\mathrm{argmax}}_{\theta} z \log P(x<\phi \mid \theta)
                         + \sum_{i=1}^n \log P(x_i \mid \theta)
$$

Specifically, when fitting a Gumbel distribution, the log likelihood to
optimize is:

$$
  \log L(\lambda, \mu) =
    n \log \lambda
     - z e^{-\lambda(\phi - \mu)}
     - \sum_{i=1}^{n} \lambda(x_i - \mu)
     - \sum_{i=1}^{n} e^{-\lambda(x_i - \mu)} \tag{11}
$$

To optimize this, we follow a similar procedure as used for complete
data [Lawless82]. The log likelihood is differentiable with respect to
$\lambda$ and $\mu$:

$$
\frac{\partial \log L}{\partial \mu} =
n \lambda
- z \lambda e^{-\lambda (\phi - \mu)}
- \lambda \sum_{i=1}^{n} e^{-\lambda (x_i - \mu)} \tag{12}
$$
$$
\frac{\partial \log L}{\partial \lambda} =
\frac{n}{\lambda}
+ z (\phi - \mu) e^{-\lambda (\phi - \mu)}
- \sum_{i=1}^{n} (x_i - \mu)
+ \sum_{i=1}^{n} (x_i - \mu) e^{-\lambda (x_i - \mu)} \tag{13}
$$

Setting (12) to zero and solving for $\hat{\mu}$ in terms of
$\hat{\lambda}$ gives:

$$
\hat{\mu}  =  - \frac{1}{\hat{\lambda}}
	\log \left[ \frac{1}{n}
	\left( z e^{-\hat{\lambda} \phi}
               + \sum_{i=1}^{n} e^{-\hat{\lambda} x_i} \right)
	\right] \tag{14}
$$

Substituting (14) into (13) gives the target equation:

$$
\frac{1}{\hat{\lambda}}
- \frac{1}{n} \sum_{i=1}^{n} x_i +
\frac{z \phi e^{-\hat{\lambda} \phi} + \sum_{i=1}^{n} x_i e^{-\hat{\lambda} x_i}}
     {z e^{-\hat{\lambda} \phi} + \sum_{i=1}^{n} e^{-\hat{\lambda} x_i}}
 =  0 \tag{15}
$$

To use Newton-Raphson root finding (instead of a slower bisection
search) we also need the first derivative of this target equation with
respect to $\lambda$:

$$
\frac{d}{d\hat{\lambda}} =
\frac{\left(
        z \phi e^{-\hat{\lambda} \phi}
        + \sum_{i=1}^{n} x_i e^{-\hat{\lambda} x_i}
       \right)^2 }
     {\left(
        z e^{-\hat{\lambda} \phi}
        + \sum_{i=1}^{n} e^{-\hat{\lambda} x_i}
       \right)^2 }
-
\frac{z \phi^2 e^{-\hat{\lambda} \phi} + \sum_{i=1}^{n} x_i^2 e^{-\hat{\lambda} x_i}}
     {z  e^{-\hat{\lambda} \phi} + \sum_{i=1}^{n} e^{-\hat{\lambda} x_i}}
-
\frac{1}{\hat{\lambda}^2} \tag{16}
$$

In summary: given $n$ observed samples $x_1..x_n$ from a total sample
of $N$ samples, $z = N-n$ of which were censored because they have
values $< \phi$, we solve for maximum likelihood estimates
$\hat{\lambda}$ and $\hat{\mu}$ using the same procedure we used for
complete data, by using equations (14), (15), and (16) in place of
equations (8), (9), and (10). Easel implements this procedure in
`esl_gumbel_FitCensored()`. The target function (15) and its
derivative (16) are implemented in the auxiliary function
`lawless422()` [Lawless82].

Results on 500 simulated datasets with $\mu = -20, \lambda = 0.4$,
censored at $\phi = -20$ -- the expected peak of the histogram; that
is, a censored fit only to the right tail, which contains about 63% of
the samples (columns are the # samples in EVD histogram):

|                        | 100 | 1000  | 10,000 | 100,000 |
|------------------------|-----|-------|--------|---------|
| % error in $\mu$       | 1%  | 0.4%  | 0.1%   | 0.04%   |
| max error in $\mu$     | 5%  | 2%    | 0.5%   | 0.2%    |
| % error in $\lambda$   | 9%  | 3%    | 0.9%   | 0.3%    |
| max error in $\lambda$ | 33% | 11%   | 3%     | 1%      |


### maximum likelihood fitting to truncated data

A _truncated_ dataset is when we only observe $n$ samples $x_i$, and an
_unknown_ number $z$ of samples less than some threshold $\phi$ were
unobserved. Thus, only the right tail of $n$ samples $x_i \geq \phi$ as
observed. In a truncated dataset, $x_1..x_n$, $n$, and $\phi$ are
known, but $z$ is unknown.

Solving a truncated data problem motivates a Bayesian approach, because
we need to integrate out (marginalize) the nuisance $z$ parameter, and
to do this, we have to specify a prior distribution for $P(z)$. Gelman
_et al._ describe a general Bayesian framework for thinking about
various types of missing data problems, including censored and
truncated data [Gelman95].

In short, to obtain maximum likelihood parameters $\hat{\theta}$ for
some distribution, given truncated data, the log likelihood we wish to
maximize is:

$$
  \hat{\theta} = \mathop{\mathrm{argmax}}_\theta -n \log P(x \geq \phi \mid \theta)
                   + \sum_{i=1}^n \log P(x_i \mid \theta). \tag{17}
$$

**Detour: derivation of the truncated data likelihood**

The derivation of the above equation may not be immediately obvious.
The presence of the $n P(x \geq \phi \mid \theta)$ term may be
counterintuitive, as opposed to the more intuitive $z P(x < \phi \mid
\theta)$ term that accounts for the missing data in a censored data
problem. Gelman _et al._ actually don't even show the equation in their
book; I obtained it from an exercise solution on their web site. To
convince you (and to remind me) of its correctness, a sketch of the
derivation follows.

We start with the same likelihood equation that arises in censored data
for a _known_ total number of samples $N$ (where $N=n+z$), but since
$N$ is unknown, we need to integrate over all possible $N$ from $n$ to
$\infty$:

$$
\begin{aligned}
   P(\mathbf{x} \mid \theta, \phi) & =
    \sum_{N=n}^{\infty}   P(\mathbf{x} \mid \theta, \phi, N) P(N)\\
   & =
    \prod_{i=1}^n P(x_i \mid \theta)
    \left[
      \sum_{N=n}^\infty {N \choose n} P(x < \phi \mid \theta)^{N-n} P(N)
    \right]\\
\end{aligned}
$$

The $\prod_{i=1}^n P(x_i \mid \theta)$ is straightforward; that sum is
our problem. The trick is to rearrange it so we can treat it as a
convergent negative binomial series:

$$
   (1-p)^{-a} = 1 + ap + \frac{a(a+1)}{2!} p^2 +
   \frac{a(a+1)(a+2)}{3!} p^3...
$$

To get the sum into the form of this series, Gelman _et al._ suggest
using an informative prior $P(N) = \frac{1}{N}$, an apparently
unmotivated choice that happens to make the sum collapse nicely:

$$
\begin{aligned}
 &= P(N=n)
    + (n+1) P(x < \phi \mid \theta) P(N=n+1)
    + \frac{(n+1)(n+2)}{2!} P(x < \phi \mid \theta)^2 P(N=n+2) ...\\
 &= \frac{1}{n} \left[
      1
      + n P(x < \phi \mid \theta)
      + \frac{n(n+1)}{2!} P(x < \phi \mid \theta)^2
      + \frac{n(n+1)(n+2)}{3!} P(x < \phi \mid \theta)^3 \right]\\
 &= \frac{1}{n} (1 - P(x < \phi \mid \theta))^{-n}\\
 &= \frac{1}{n} P(x \geq \phi \mid \theta)^{-n}\\
\end{aligned}
$$

The $\frac{1}{n}$ is a constant, so we drop it from the likelihood
equation we'll maximize. Putting this term back together with the
probability of the observed data and taking the log, we obtain the log
likelihood in equation (17).

Alternatively, we might choose an uninformative improper uniform prior
$P(N) \propto 1$. This gives a log likelihood that only differs by a
term of $n+1$ versus $n$:

$$
  \hat{\theta} = \mathop{\mathrm{argmax}}_\theta -(n+1) \log P(x \geq \phi \mid \theta)
                   + \sum_{i=1}^n \log P(x_i \mid \theta). \tag{18}
$$

However, empirically, this form is ineffective, at least for fitting
Gumbels. The $\frac{1}{N}$ prior performs much better, probably because
it constrains the solutions to favor smaller, finite, more reasonable
choices of $N$.

**Back to fitting a truncated Gumbel**

For the specific case of fitting a truncated Gumbel, the log likelihood
(17) to optimize is:

$$
  \log L(\lambda, \mu) =
     n \log \lambda
     - \sum_{i=1}^{n} \lambda(x_i - \mu)
     - \sum_{i=1}^{n} e^{-\lambda(x_i - \mu)}
     - n \log (1 - \exp(-e^{-\lambda(\phi - \mu)})) \tag{19}
$$

This is differentiable with respect to $\lambda$ and $\mu$, but it's
not going to reduce to the clean root-finding problem that we obtained
for complete data or censored data. Instead we're going to be left with
a numerical optimization problem. We can use standard numerical
optimization code, such as steepest descent or conjugate gradient
descent. There's just one hitch. These algorithms assume unconstrained
parameters, but $\lambda$ is constrained to values $>0$. We do a change
of variables, and use the transformation $\lambda = e^w$ so we can
optimize the unconstrained parameter $w = \log \lambda$ instead of
optimizing $\lambda$ directly. The necessary partial derivatives are
then:

$$
\frac{\partial \log L}{\partial \mu} =
n \lambda
- \lambda \sum_{i=1}^{n} e^{-\lambda (x_i - \mu)}
- \frac{n \lambda \exp \left[ -\lambda (\phi - \mu) - e^{- \lambda (\phi - \mu)} \right]}
       {1 - \exp(-e^{-\lambda(\phi - \mu)})} \tag{20}
$$
$$
\frac{\partial \log L}{\partial w} =
n
- \sum_{i=1}^{n} \lambda(x_i - \mu)
+ \sum_{i=1}^{n} \lambda(x_i - \mu) e^{-\lambda (x_i - \mu)}
+ \frac{n\lambda (\phi-\mu) \exp \left[ -\lambda (\phi - \mu) - e^{- \lambda (\phi - \mu)} \right]}
       {1 - \exp(-e^{-\lambda(\phi - \mu)})} \tag{21}
$$

This optimization is carried out by `esl_gumbel_FitTruncated()`. The
likelihood (19) is implemented in `tevd_func()`, and the derivatives
(20) and (21) are implemented in `tevd_dfunc()`.
`esl_gumbel_FitTruncated()` simply sets up the problem and passes it
all off to a conjugate gradient descent optimizer.

Results on 500 simulated datasets with $\mu = -20, \lambda = 0.4$,
truncated at $\phi = -20$ (leaving the right tail, containing about 63%
of the samples; columns are the # samples):

|                              | 100  | 1000  | 10,000 | 100,000 |
|------------------------------|------|-------|--------|---------|
| % error in $\hat{\mu}$       | 13%  | 2%    | 0.8%   | 0.3%    |
| max error in $\hat{\mu}$     | 260% | 42%   | 3%     | 1%      |
| % error in $\hat{\lambda}$   | 15%  | 5%    | 2%     | 0.6%    |
| max error in $\hat{\lambda}$ | 68%  | 18%   | 6%     | 2%      |

Fitting truncated Gumbel distributions is difficult, requiring much
more data than fitting complete or censored data. The problem is that
the right tail becomes a scale-free exponential when $\phi >> \mu$, and
$\mu$ becomes undetermined. Fits become very inaccurate as $\phi$ gets
larger than $\mu$, and for sufficiently large $\phi$, the numerical
optimizer will completely fail.


## References

* **[Altschul01]** SF Altschul, R Bundschuh, R Olsen, T Hwa, ["The
  estimation of statistical parameters for local alignment score
  distributions"](https://pubmed.ncbi.nlm.nih.gov/11139604/),
  _Nucleic Acids Research_ 29:351-361 (2001).

* **[Evans00]** M Evans, N Hastings, B Peacock, [_Statistical
  Distributions_](https://search.worldcat.org/search?q=isbn:0471371246),
  3rd ed., John Wiley & Sons (2000).

* **[Gelman95]** A Gelman, JB Carlin, HS Stern, DB Rubin, [_Bayesian
  Data Analysis_](https://search.worldcat.org/search?q=isbn:0412039915),
  Chapman & Hall/CRC (1995).

* **[Lawless82]** JF Lawless, [_Statistical Models and Methods for
  Lifetime Data_](https://search.worldcat.org/search?q=isbn:0471085448),
  John Wiley & Sons (1982).
